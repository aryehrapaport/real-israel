name = "real-israel"
compatibility_date = "2026-01-05"

# Cloudflare Pages output folder
pages_build_output_dir = "dist"

# Note: D1 bindings are configured in Cloudflare Pages UI (Settings → Functions → D1 bindings)
